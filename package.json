{
  "name": "wfill",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p packages/contracts/tsconfig.json --noEmit && tsc -p packages/rules-core/tsconfig.json --noEmit && tsc -p packages/game-engine/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}

{
  "name": "wfill",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @wfill/contracts build && pnpm --filter @wfill/rules-core build && pnpm --filter @wfill/game-engine build",
    "test:raw-package-import": "pnpm build && node packages/game-engine/test/raw-package-import.mjs",
    "test": "vitest run",
    "typecheck": "pnpm build && tsc -p packages/contracts/tsconfig.json --noEmit && tsc -p packages/rules-core/tsconfig.json --noEmit && tsc -p packages/game-engine/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
